export interface ExperienceModel {
    company: string;
    startDate: string;
    endDate: string;
    city?: string;
    country?: string;
    role: string;
    description: string;
    descriptionList: string[];
}